export function generateRoadmap(topic: string) {
  // Simple stub — replace with actual generation logic
  return {
    topic,
    plan: [`Overview of ${topic}`, 'Learning objectives', 'Week-by-week tasks']
  }
}
